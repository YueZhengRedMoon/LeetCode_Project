#include <iostream>
#include <vector>

class Solution
{
public:
    void wiggleSort(std::vector<int> &nums)
    {
        // 重新排序后的数组，偶数索引处的数比两边的数小，奇数索引处的数比两边的数大

    }
};

int main()
{
    std::cout << "For Kirie!" << std::endl;
    return 0;
}
